import 'package:flutter/material.dart';

class PublicContract extends StatelessWidget {
  const PublicContract({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(child: Text('ПУБЛІЧНИЙ ДОГОВІР ( ДОГОВІР ПУБЛІЧНОЇ ОФЕРТИ)'),);
  }
}
